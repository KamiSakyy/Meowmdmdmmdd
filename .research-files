# Классы для полного дампа (подстрока пути; максимум 5 файлов на строку)
# Подэкраны MDGram-настроек (кандидаты на кнопки):
MDsettings/u55
/u55.smali
/u55$
MDsettings/m55
/m55.smali
/m55$
# HTTP-хелпер апдейтера:
tw3
# Интерфейс колбэков сети (для призрака):
tgnet/a.smali
# Экран чата (кнопка стикеров) — большой, обрежется до 120КБ:
ui/ChatActivity.smali
