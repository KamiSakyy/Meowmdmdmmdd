# 📊 MDGram Revival — прогресс (обновляется каждый заход)

> Цель: **чистый полный Gradle-исходник MDGram**, из которого собирается APK
> и который можно обновлять (мержить новые версии Telegram).
> Дата последнего обновления: 2026-09-09.

## Сводка

| # | Фаза | Готово | Что осталось |
|---|---|---|---|
| 0 | Декомпиляция APK 9.3.3 (apktool + jadx) | **100 %** | — |
| 1 | Карта обфускации (классы / ресурсы / методы) | **45 %** | методы и поля движка |
| 2 | Порт МД-слоя (Java, 29 файлов) | **25 %** | 21 файл (сделаны Views/* + NoScrollListView) |
| 3 | Порт МД-патчей движка (63 флага MDConfig) | **3 %** | найти точки хуков в чистом исходнике |
| 4 | Ресурсы (темы, шрифты, иконки, строки, макеты) | **60 %** | стили/темы/шрифты/макеты движка |
| 5 | Gradle-проект (структура, зависимости, манифест) | **40 %** | манифест МД (активности, алиасы иконок, провайдеры) |
| 6 | Сборка APK (GitHub Actions) | **0 %** | workflow + первая сборка |
| 7 | Обновляемость (мерж апстрима, релизы) | **0 %** | стратегия обновлений |

**ИТОГО: ~23 %**

## Что сделано в этом заходе

- ✅ `tools/deobf-map.py` — автокарта обфускации классов:
  сопоставление по строковым литералам + дескрипторам методов + суперклассу.
  Результат: `tools/deobf-map.json` — **253 класса** движка опознано из 391
  (`org.telegram.*`), плюс 40 классов из `defpackage`.
- ✅ `tools/deobf-methods.py` — помощник маппинга методов (точно там, где в
  методе есть строки; для «пустых» методов нужен ручной разбор).
- ✅ `tools/r-lookup.py` — расшифровка обфусцированных ресурсов
  (`i68.z0` → `R.id.mdGeneral`). Работает по `public.xml` из apktool.
- ✅ Расшифрован главный экран настроек `MDsetting`: все строки, id, макет.
- ✅ В `MDGram/app/build.gradle` добавлены зависимости МД-библиотек (RWidgetHelper,
  AndroidUtilCode, BRVAH, FlycoTabLayout, Moxy, EventBus, ProcessPhoenix, CustomActivityOnCrash).
- ✅ Портированы первые файлы МД-слоя в чистый код: `Views/TextView`, `TextViewBold`,
  `TextViewNormal`, `TextViewBoldAccent`, `MDView`, `MDImageView`, `fontStyle/NoScrollListView`.
- ✅ `PORT_PLAN.md` + `tools/port-status.py` — инвентарь порта со статусами.

## Ключевые факты (запомнить)

- Базовый исходник в `android/` — это **Telegram 9.3.3** (`BuildVars.BUILD_VERSION_STRING = 9.3.3`),
  с небольшой добавкой `org.telegram.ui.LNavigation` (не чистый DrKLO — проверить происхождение!).
- APK MDGram 9.3.3 (30275) = Telegram 9.3.3 + МД-слой, обфусцированный R8.
  Структура пакетов `org.telegram.*` сохранена, имена классов — нет.
- R-классы в декомпиле: `e78=R.string`, `i68=R.id`, `g68=R.drawable`, `v68=R.layout`.
- Сторонние библиотеки МД: Ruffian RWidgetHelper, AndroidUtilCode (blankj),
  BRVAH, FlycoTabLayout, Moxy, EventBus, ProcessPhoenix, CustomActivityOnCrash.

## Дальнейшие шаги (порядок)

1. Порт `MDsetting` + 5 подэкранов настроек (`m55`,`d55`,`o55`,`s55`,`u55`).
2. Порт `Views/*`, `fontStyle/*`, `ChatHelper/*`, `Activies/Ab|AbV`.
3. Разбор и порт 63 флагов `MDConfig` на чистый движок.
4. Манифест: активности МД, алиасы иконок, провайдеры.
5. Workflow сборки APK в Actions → первый APK.
6. Стратегия обновлений (мерж апстрима).
