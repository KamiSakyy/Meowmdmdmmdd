# ИНСТРУКЦИЯ — MDGram 9.3.3: скачивание APK и декомпиляция

> Всё ниже уже сделано автоматически в ветке `arena/01a08582-meowmdmdmmdd`.
> APK взят **со стороннего зеркала (APKPure), НЕ с GitHub**.

---

## 1. Откуда взят APK

- **Источник:** APKPure — страница «MDGram Messenger 9.3.3»
  `https://apkpure.com/mdgram-messenger/org.telegram.mdgram/download/9.3.3`
- **Файл:** MDGram Messenger **9.3.3** (versionCode **30275**), universal APK **75.6 МБ**
- **Дата выхода:** 28.01.2023. Чейнджлог: «MDGram V15.5 + Base Updated Telegram 9.3.3…»
- **Подлинность:** SHA1 файла сверен при скачивании:
  `4fa5ae96754b910045d1d84dac7a8261c59793e9`
- **Запасной источник:** Aptoide (тот же 9.3.3 / 30275), включается параметром `apk_source=aptoide`.

Скачанный APK лежит в **Releases** этого репозитория: тег **`apk-9.3.3`**.

## 2. Как выполнена декомпиляция

Workflow **`.github/workflows/decompile.yml`** (запуск — push в эту ветку):

1. Качает APK с APKPure, проверяет SHA1.
2. Ставит `apktool 2.9.3` → ресурсы, `AndroidManifest.xml`, smali.
3. Ставит `jadx 1.4.7` → Java-код.
4. Собирает выжимку в папку **`decompiled/`** и коммитит её в ветку.
5. Полный вывод (весь jadx + весь apktool) кладёт в **артефакты** рана (Actions → run → Artifacts, хранятся 90 дней).
6. APK прикрепляет к релизу `apk-9.3.3`.

## 3. Что лежит в `decompiled/`

| Файл/папка | Что это |
|---|---|
| `APK_INFO.md` | источник, хеши (SHA1/SHA256/MD5), версия, статистика |
| `AndroidManifest.xml` | декодированный манифест (пакет `org.telegram.mdgram`) |
| `apktool.yml` | версия/код версии из сборки |
| `strings.xml` | все строки приложения |
| `md-strings-keys.txt` | ключи строк MDGram (`md_*`, `MD_*`, `rc_*`) |
| `themes/*.attheme` | все темы MDGram (Material You `monet_*`, `ios*` и др.) |
| `fonts-list.txt` | список шрифтов из APK |
| `java/org_telegram_mdgram/` | фирменный Java-слой MDGram (`MDsettings`, экраны, вью) |
| `java/ApplicationLoaderImpl.java` | точка входа приложения |
| `smali-mdgram/` | тот же MDGram-слой в smali (имена как есть) |
| `mdconfig_flags.txt` | все флаги модов из `MDConfig` |

## 4. Как перезапустить декомпиляцию

Вариант А (сейчас): поменяй файл `.decompile-trigger` (любой текст) и запушь в эту ветку —
workflow стартует сам.

Вариант Б (после мержа в `main`): Actions → «Декомпиляция MDGram 9.3.3» →
**Run workflow** → можно выбрать `aptoide` вместо `apkpure`.

## 5. Как скачать результаты

- **Выжимка:** папка `decompiled/` прямо в ветке (git pull).
- **Полный декомпил:** вкладка **Actions** → последний зелёный ран → **Artifacts** →
  `mdgram-933-decompiled` (`full-jadx.zip` + `full-apktool.zip` + APK).
- **APK:** вкладка **Releases** → `apk-9.3.3` → `MDGram-9.3.3.apk`.

## 6. Что дальше (возрождение)

1. Декомпилированный код 9.3.3 — это **референс**, а не основа: база Telegram с тех пор
   сильно ушла вперёд (stories, mini-apps, новый API).
2. Стратегия: берём **свежий исходник Telegram** (официальный или живой форк),
   переносим 1-в-1 **ресурсы** (темы, шрифты, иконки, строки) и **переписываем**
   слой модов по `mdconfig_flags.txt` + `java/org_telegram_mdgram/`.
3. Сборка нового APK — следующим workflow (`build.yml`), позже в этой же ветке.
4. Для входа в аккаунт нужны **свои** `api_id`/`api_hash` с https://my.telegram.org/apps,
   приложение нельзя называть «Telegram» (правила Telegram для сторонних клиентов).

Подробный разбор содержимого APK — в `MDGRAM_ANALYSIS_RU.md`,
память/план проекта — в `MEMORY_AND_PLAN.md`.

## 7. Мод-сборка (новое название, без «нового обновления»)

- Workflow **`.github/workflows/mod.yml`**: качает APK с APKPure → apktool →
  патчи → пересборка → подпись нашим ключом → релиз.
- **Патчи (дизайн НЕ меняется, API-ключи родные из APK):**
  1. Название приложения → «MDGram Revival» (все языки);
  2. URL OTA-проверки обновления → `127.0.0.1` (попап «новое обновление» не появляется);
  3. Экран About снова показывает «OFICIAL» как в оригинале (подпись теперь наша).
- **Отчёт:** `mod/BUILD_REPORT.md` + `mod/PATCH_REPORT.txt` в ветке.
- **Ключ подписи:** `mod-keystore/` (лежит в ветке, чтобы следующие сборки
  ставились поверх этой).
- **APK мода:** Releases → **`mod-9.3.3`** → `MDGram-9.3.3-mod.apk`.
- ⚠️ **Перед установкой мода УДАЛИ оригинальный MDGram** (подпись другая —
  поверх не встанет, будет ошибка установки).
- Перезапуск сборки: поменяй файл `.mod-trigger` и запушь.
